# Vite (frontend) env vars
# Get a RapidAPI key from https://rapidapi.com/ and set the host per the API you use
VITE_API_KEY=your_rapidapi_key_here
VITE_API_HOST=crypto-news16.p.rapidapi.com
# Backend API URL - leave empty for local dev, set to your Render backend URL for production
# Example: VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=

# Backend (Express) env vars
# If you use the built-in Node server for auth, set these before starting it
PORT=3001
JWT_SECRET=change_me_to_a_secure_random_string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxxx.mongodb.net/<db>?retryWrites=true&w=majority
DB_NAME=cryptox
